# Integration Diagram Generator

This small Single Page Application (SPA) allows you to choose integration building blocks
(API Gateway, ESB, message queues, etc.) between a source and destination system.
After selecting the desired components, a diagram is rendered using [Mermaid](https://mermaid.js.org/).

Open `index.html` in a web browser to use the tool.
